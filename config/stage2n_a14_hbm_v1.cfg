# Stage 2N-A14 phase-1 HBM embedding architecture contract.
# This is metadata for the frozen layout, not yet a Vitis link configuration.
# No A14 m_axi port exists, so no connectivity.sp binding is declared here.

hbm_bank=HBM[0]
embedding_rows=64
embedding_dimension=8
element_width_bits=16
vector_width_bits=128
row_stride_bytes=16
base_byte_address=0x00000000
lookup_mode=single_bank_direct
