# 第 10 批文献（2026-09-24）

远程分支 `work/stage2n-a16-multibank-parallel` 尖端是 `ac836f3`（D-091）。`main` 停在更早的 A13 快照，不是这条研究线。2026-09-21 到 09-24 的周期账本和 D-100..D-108 没有出现在这个远程上；下面的 1174 拆分沿用已转移的对话，不是本轮重测。

仿真 golden **36**。板上功能 golden **−393**。二者不要混用。`PERFORMANCE=NOT_CLAIMED`。

几何：Bottom 8→16→8，交互 5×8→18，Top 18→32→16→1。权重 1360，偏置 73，描述符 5 个。共享稠密引擎 `NUM_PE=16`。1174 = 乘法发射 121 + 归约/量化 584（73×8）+ 取指/偏置/命令/边沿 350 + 阶段收尾 11 + 交互 100 + 装入 8。查找中位 33，最大约 56–65。同 16 个 PE 把下一输出的发射叠进上一输出的 8 拍，窗口只有 6 拍，1174→788（握手不能同时取指则为 834）。584 拍仍在。这篇不重开那条线，也不写 RTL。

第 10 批只留能改这 8 拍、或用现有数字杀掉的一句：

- RECom：删的是框架边界检查，以及 one-hot 被当成 multi-hot 才存在的 embedding 归约。这条图里没有这两样。
- LCR/LARU：改的是 GPU embedding 或 KV 缓存的替换。查找 33 盖不住 584，热缓存已停。
- Shi、Kara、Hagleitner：HBM 用来做分析扫描、连接和 SGD。1360 个权重已在片上供给器里。
- CARINA：热 embedding 放 DRAM，并按 CXL 带宽拆任务。动的是表流量，不是片上这 8 拍。
- iMARS：全连接放进 FeFET 交叉开关。那一步离开逐位 INT16 合同。
- HiLFS：给 HLS 做片上文件系统。1174 拍里没有文件访问。
- Boutros、More、Betz：要的是下一颗器件上的硬张量块和硬网络，不是把现有 16 个 PE 的 8 拍排短。
- Frugal：训练时把别的 GPU 下一步要用的 embedding 参数提前刷到主机，让出前台口。这是训练通信，不改推理的 73×8。
- Mem-GF：用 Krylov 子空间免得存物品相似度矩阵。对象是图协同过滤，不是这块稠密引擎。
- Karimzadeh 等：综述十亿参数模型的存内计算挑战，没有更短的数字归约。

没有新支线。没有 RTL，没有上板，没有改已验收 RTL。
