# Risk register

| ID | Risk | Probability | Impact | Current evidence | Mitigation | Stop condition |
|---|---|---|---|---|---|---|
| R-001 | RTL elaboration defect | Low | High | Retry2: `xvlog` and 8/8 `xelab` PASS under Vivado 2020.2 | Preserve the proven source subset and ordered validation flow | A new elaboration error appears after a source change |
| R-002 | Vivado 2020.2 SystemVerilog incompatibility | Low | High | Retry2 compiles, elaborates, and simulates all eight designs | Continue Vivado 2020.2 regression after RTL changes | A required change cannot be represented in the supported subset |
| R-003 | Signed width/rounding mismatch | Low | High | Python 24-vector and XSim top 24-vector bit comparisons PASS | Retain fixed-point contract and directed boundary vectors | New evidence disagrees with the frozen fixed-point specification |
| R-004 | Ready/valid loss or duplication | Low | High | FIFO replacement/depth-one/random tests, dot/embedding consecutive replacement, and integration backpressure all PASS | Retain all explicit PASS/timeout/data-stability checks | Regression reports loss, duplication, overwrite, or unstable output |
| R-005 | Testbench scheduling race or false PASS | Low | High | Retry2 has 8/8 explicit PASS markers; no fatal/timeout/error/warning | Require PASS marker plus exit/status/log audit on every run | A tool exits zero without the expected marker or scheduling becomes nondeterministic |
| R-006 | Source/log version mismatch | Low | High | Server Git is UNKNOWN, but all 64 manifest hashes, including 17 RTL/TB files, match commit `44a1b25` | Keep payload SHA manifest and audit against an explicitly named commit | Returned evidence lacks a complete matching source manifest |
| R-007 | INT32 wrap harms trained accuracy | High for full-range D=512 | High | Exact bound needs 33 bits for INT16×INT8 D=512 plus INT24 bias; trained distribution unknown | Retain ACC32 compatibility and implement ACC48 candidate mode; require trained range/accuracy review | Model adoption is requested without bit-accurate/range evidence |
| R-008 | Parameterized PE architecture may not meet F37X resources/timing | High | High | Analytical P=16 model only; no synthesis/resource/timing evidence exists | Registered MAC/reduction/quantize stages; synthesize P=4/8/16/32 only after architecture approval | P=16 cannot meet a reviewed clock/resource bound |
| R-009 | HBM behavior invalidates scheduling assumptions | High | High | No AXI/HBM is implemented | Defer to GATE-4 and require shell/platform evidence | Real HBM mapping/interface choice is required |
| R-010 | Weight provider cannot sustain PE demand | Medium-High | High | P=16 needs 128 weight bits/cycle during MAC; P=32 needs 256 | Elastic abstract provider, explicit starvation counters, stall-safe control; review cache/tiling only after baseline | Measured useful-MAC utilization remains supply-bound |
| R-011 | Runtime dimensions or tail mask cause out-of-range/wrong products | Medium | High | Python mask/schedule matrix passes P=4/8/16/32 and D<P/non-divisible cases; RTL not run | Masked testbenches, address checks, random D/O/P, real XSim P matrix | Any masked lane changes arithmetic or accesses invalid storage |
| R-012 | Ping-pong ownership/backpressure creates deadlock or overwrite | Medium | High | RTL implements copied descriptors, distinct buffer selectors, elastic responses/FIFO; only static/Python checks exist | Independent buffer/provider tests, same-edge replacement, randomized result stalls, server XSim | A legal ready/valid pattern can deadlock or corrupt a context |
| R-013 | Per-layer scale/activation values are not trained-model approved | High | High | Descriptor fields are approved, but only shift=4 plus ReLU is validated | Test interface range while using v0 values for compatibility; require trained-model review before full MLP | A formal layer cannot preserve the external format with an approved shift/ReLU setting |
| R-014 | 2-D PE enhancement exceeds routing/memory benefit | High | Medium-High | 16×4 already requires 512 weight bits/cycle and 64 multipliers | Defer Scheme C until Scheme B post-synthesis/utilization evidence exists | Output parallelism is bandwidth- or timing-limited |
| R-015 | ACC48 fails timing or uses unexpected LUT/DSP resources | High | High | Arithmetic bound is safe, but no Vivado synthesis exists and DSP mapping is deliberately unclaimed | Keep ACC_WIDTH parameterized; synthesize ACC32/48 with P=4/8/16/32 after functional validation | ACC48 cannot meet the reviewed clock/resource envelope |
| R-016 | Stage-2A RTL has no local simulator evidence | Certain locally | High | Unified run: Icarus, Verilator, Verible, Stage-1 XSim, and Stage-2A XSim all explicitly SKIPPED | Exact-source XSim payload and stop for server logs | Any RTL PASS/GATE claim is made without returned real logs |
| R-017 | Descriptor/provider error paths create partial jobs | Medium | High | Python rejects seven boundary descriptors; engine/testbench implement explicit dimension/buffer/shift/provider errors, but RTL is not run | Reject before requests, explicit error status, assertions that invalid jobs emit no result | An invalid job changes buffer/provider/output state |
| R-018 | Synthetic trace locality overstates real coalescing value | High | High | No user-supplied Criteo trace is present | Label synthetic results preliminary; require GATE-T1 on real local data | Hardware work is proposed from synthetic-only reduction rates |
| R-019 | Channel scheduler optimizes an artificial imbalance | Medium-High | High | Channel mapping is currently abstract and trace-free | Compare balanced/skewed/hotspot-shift validation traces, then real static mapping before scheduling | Static mapping is already balanced or improvement is negligible |
| R-020 | Local PyTorch dependency is unavailable | Certain in current bundled runtime | Medium | Python 3.12 has NumPy/Pandas but no `torch`; network/install is forbidden | Implement standard PyTorch path, use independent NumPy oracle for local structural validation, mark PyTorch CPU/CUDA SKIPPED | Any PyTorch execution is reported PASS without an installed runtime |
| R-021 | Benchmark timing mixes data loading with forward time | Medium | High | New benchmark tooling is not yet validated | Separate warmup, end-to-end, forward-only, explicit CUDA synchronization, JSON/CSV raw fields | Kernel-only claims include file/data-loader cost |
| R-022 | Fan-out metadata/window state is not hardware-bounded | Medium | High | Coalescer exists only as a planned simulator | Report max requests, unique IDs, fan-out tags and state bytes for every policy | A candidate requires unbounded state or reordering |
| R-023 | Real Criteo schema/path differs from the local loader contract | Medium | Medium | No real dataset is present | Strict local-path/schema validation, helpful diagnostics, no automatic download | User data cannot be loaded without undocumented conversion |

## PE architecture review — five highest risks

1. **R-007:** INT32 is not mathematically safe for the full-range 512-input case.
2. **R-010:** provider delivery may underfeed the selected P.
3. **R-008:** modeled P=16 timing and DSP/BRAM mapping are not synthesis facts.
4. **R-011:** tail masks and runtime dimensions can silently corrupt dot products.
5. **R-012:** multi-channel backpressure and ping-pong ownership can deadlock or
   overwrite data without formal invariants and randomized stall testing.
