# Risk register

| ID | Risk | Probability | Impact | Current evidence | Mitigation | Stop condition |
|---|---|---|---|---|---|---|
| R-001 | RTL elaboration defect | Low | High | Retry2: `xvlog` and 8/8 `xelab` PASS under Vivado 2020.2 | Preserve the proven source subset and ordered validation flow | A new elaboration error appears after a source change |
| R-002 | Vivado 2020.2 SystemVerilog incompatibility | Low | High | Retry2 compiles, elaborates, and simulates all eight designs | Continue Vivado 2020.2 regression after RTL changes | A required change cannot be represented in the supported subset |
| R-003 | Signed width/rounding mismatch | Low | High | Python 24-vector and XSim top 24-vector bit comparisons PASS | Retain fixed-point contract and directed boundary vectors | New evidence disagrees with the frozen fixed-point specification |
| R-004 | Ready/valid loss or duplication | Low | High | FIFO replacement/depth-one/random tests, dot/embedding consecutive replacement, and integration backpressure all PASS | Retain all explicit PASS/timeout/data-stability checks | Regression reports loss, duplication, overwrite, or unstable output |
| R-005 | Testbench scheduling race or false PASS | Low | High | Retry2 has 8/8 explicit PASS markers; no fatal/timeout/error/warning | Require PASS marker plus exit/status/log audit on every run | A tool exits zero without the expected marker or scheduling becomes nondeterministic |
| R-006 | Source/log version mismatch | Low | High | Server Git is UNKNOWN, but all 64 manifest hashes, including 17 RTL/TB files, match commit `44a1b25` | Keep payload SHA manifest and audit against an explicitly named commit | Returned evidence lacks a complete matching source manifest |
| R-007 | INT32 wrap harms trained accuracy | High for full-range D=512 | High | Exact bound needs 33 bits for INT16×INT8 D=512 plus INT24 bias; trained distribution unknown | Retain ACC32 compatibility and implement ACC48 candidate mode; require trained range/accuracy review | Model adoption is requested without bit-accurate/range evidence |
| R-008 | Parameterized PE architecture may not meet F37X resources/timing | High | High | Local P=16 Artix-7 OOC synthesis now meets 100 MHz at +0.758 ns with 4,550 LUT, 1,946 FF, 17 RAMB36, 32 RAMB18, and 21 DSP; this is not F37X evidence | Retain the reproducible local review and require target-part evidence after approval | P=16 cannot meet a reviewed target clock/resource bound |
| R-009 | HBM behavior invalidates scheduling assumptions | High | High | No AXI/HBM is implemented | Defer to GATE-4 and require shell/platform evidence | Real HBM mapping/interface choice is required |
| R-010 | Weight provider cannot sustain PE demand | Medium | High | Local synthesis infers 16 independent 4096x8 RAMB36 banks and XSim passes aligned, unaligned, cross-row, masked, and continuous reads; external refill remains abstract | Elastic provider and stall-safe control; review refill/cache/tiling only after target evidence | Measured useful-MAC utilization remains supply-bound |
| R-011 | Runtime dimensions or tail mask cause out-of-range/wrong products | Medium | High | Python mask/schedule matrix passes P=4/8/16/32 and D<P/non-divisible cases; RTL not run | Masked testbenches, address checks, random D/O/P, real XSim P matrix | Any masked lane changes arithmetic or accesses invalid storage |
| R-012 | Ping-pong ownership/backpressure creates deadlock or overwrite | Low-Medium | High | Local Vivado 2022.1 passes independent buffer/provider tests and all 20 Stage 2B cases, including same-edge replacement and result stalls; target-version evidence is pending | Retain directed and randomized stalls and rerun exact-source Vivado 2020.2/XSim | A legal ready/valid pattern can deadlock or corrupt a context |
| R-013 | Per-layer scale/activation values are not trained-model approved | High | High | Descriptor fields are approved, but only shift=4 plus ReLU is validated | Test interface range while using v0 values for compatibility; require trained-model review before full MLP | A formal layer cannot preserve the external format with an approved shift/ReLU setting |
| R-014 | 2-D PE enhancement exceeds routing/memory benefit | High | Medium-High | 16×4 already requires 512 weight bits/cycle and 64 multipliers | Defer Scheme C until Scheme B post-synthesis/utilization evidence exists | Output parallelism is bandwidth- or timing-limited |
| R-015 | ACC48 fails timing or uses unexpected LUT/DSP resources | Medium | High | Default local ACC48/P=16 OOC synthesis meets 100 MHz with 21 DSP after explicit DSP-product/fabric-accumulator pipelining; other P/target combinations remain unmeasured | Keep ACC_WIDTH parameterized and require target-part ACC32/48 evidence | ACC48 cannot meet the reviewed target clock/resource envelope |
| R-016 | Stage-2A target-version evidence regresses after later source changes | Low for reviewed source | High | Exact-source Vivado 2020.2 passes 6/6 Stage 2A benches and Stage 2B 20/20 at source head `fff6bd8`; Vivado 2022.1 results agree | Keep the version-locked runner and require a new source-matched audit after relevant RTL/TB changes | A later source change is claimed target-version passed without matching 2020.2 evidence |
| R-017 | Descriptor/provider error paths create partial jobs | Medium | High | Python rejects seven boundary descriptors; engine/testbench implement explicit dimension/buffer/shift/provider errors, but RTL is not run | Reject before requests, explicit error status, assertions that invalid jobs emit no result | An invalid job changes buffer/provider/output state |
| R-018 | Synthetic trace locality overstates real coalescing value | High | High | Six synthetic classes run; W=8 reduction spans 0%-57.62%, proving the generator can drive opposite outcomes, but no Criteo trace exists | Keep all GATE-T statuses INCONCLUSIVE; require GATE-T1 on user-supplied local data | Hardware work is proposed from synthetic-only reduction rates |
| R-019 | Channel scheduler optimizes an artificial imbalance | Medium-High | High | Abstract 8-channel queue-aware scheduling degrades balanced/low-duplicate traces by about 1%, while synthetic skew improves strongly | Report negative as well as positive cases; require real mapping/trace before scheduling RTL | Static mapping is already balanced or improvement is negligible |
| R-020 | Local PyTorch dependency is unavailable | Certain in current bundled runtime | Medium | Standard PyTorch model/CLI exists, but CPU and CUDA tests are explicitly SKIPPED; 12 dependency-light tests pass | Use an already provisioned PyTorch environment later; never install/download autonomously or treat NumPy as PyTorch | Any PyTorch execution is reported PASS without an installed runtime |
| R-021 | Benchmark timing mixes data loading with forward time | Low-Medium | High | Tested JSON/CSV report separates warmup, preloaded host end-to-end, forward-only and four stage totals; CUDA synchronization path exists but is unrun | Retain scope metadata and rerun on a controlled PyTorch environment before baseline claims | Kernel-only claims include file/data-loader cost |
| R-022 | Fan-out metadata/window state is not hardware-bounded | Medium | High | Five policies enforce max requests/wait/unique IDs and report request/tag occupancy, fan-out and estimated state bytes | Derive RTL bounds only after real GATE-T1 sweep review | A candidate requires unbounded state or reordering |
| R-023 | Real Criteo schema/path or ID mapping differs from the loader contract | Medium | High | Temporary classic 40-column TSV passes; URLs are rejected; trace mode preserves raw hex IDs so small table modulo cannot manufacture duplicates, but no real dataset was tested | User supplies local classic TSV; validate schema, vocabulary/hash mapping and table cardinalities before model accuracy work | User data requires undocumented conversion or a trace gate uses modulo-collided test IDs |
| R-024 | Two-candidate queue-aware placement is physically infeasible or too costly | High | High | Current scheduler deliberately assumes replicated/relocatable placement and is not an F37X address map | Replace the assumption with reviewed HBM table/address placement before hardware claims | Modeled scheduler benefit depends on unavailable replication or cross-channel movement |
| R-025 | Logical lookup timing model mispredicts real HBM latency/parallelism | High | High | Fixed service/broadcast/reorder costs are abstract and no HBM trace exists | Sensitivity analysis first, then GATE-4 platform measurements | Candidate choice reverses under plausible measured latency/bandwidth |
| R-026 | Runtime quantization path prevents the reviewed clock | Low for Artix-7 OOC; unknown after implementation/on target | High | Vivado 2020.2 and 2022.1 both report WNS +0.758 ns, TNS 0, and 0 failing endpoints for the same 10 ns Artix-7 OOC configuration; functional regressions pass | Preserve latency/protocol tests and require full target implementation timing before platform claims | Implemented or target timing fails, or completion ordering changes |
| R-027 | Vivado version is misidentified during reproduction | Low with audited evidence | High | All returned synthesis reports identify Vivado v2020.2 build 3064766; the archive hash matches and the version-aware parser passes | Retain executable/version, source head, archive hash, and embedded report versions with every reproduction | A target-version result lacks matching tool/version/source evidence |

## PE architecture review — five highest risks

1. **R-007:** INT32 is not mathematically safe for the full-range 512-input case.
2. **R-010:** provider delivery may underfeed the selected P.
3. **R-008/R-026:** P=16 storage mapping and 100 MHz OOC closure agree under
   Vivado 2020.2 and 2022.1, but F37X implementation remains unmeasured.
4. **R-011:** tail masks and runtime dimensions can silently corrupt dot products.
5. **R-012:** multi-channel backpressure and ping-pong ownership can deadlock or
   overwrite data without formal invariants and randomized stall testing.
