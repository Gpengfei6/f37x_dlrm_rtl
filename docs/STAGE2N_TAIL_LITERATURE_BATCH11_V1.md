# 第 11 批文献（2026-09-24）

沿用 D-092 的账，不重测。仿真 golden **36**。板上功能 golden **−393**。二者不要混用。`PERFORMANCE=NOT_CLAIMED`。

1174 = 乘法发射 121 + 归约/量化 584（73×8）+ 取指/偏置/命令/边沿 350 + 阶段收尾 11 + 交互 100 + 装入 8。查找中位 33，最大约 56–65。同 16 个 PE 的重叠已判为普通微结构，1174→788，584 拍仍在。

第 11 批只留能改这 8 拍、或用现有数字杀掉的一句：

- nMARS：压缩后的 embedding 表放进存内交叉开关。Criteo 上加速的是表操作，离开逐位 INT16，不改 73×8。
- Piper：流式做训练数据的词表和特征预处理。训练预处理已停，1174 拍里没有这一段。
- Lim 等：embedding 元素量化约 4 倍，池化放在 3D DRAM 的逻辑层，热向量进 SRAM。查找中位 33 盖不住 584，热缓存已停。他们减的是查表后的池化，不是稠密引擎这 8 拍。
- FLASH 期刊版：仍是九种模乘模幂。这条图里没有。
- MeCache：多 GPU 上异构图训练的通信缓存。训练和图邻居都不是这 584 拍。
- More 的架构/VLSI 学位论文：给下一颗器件量硬块的面积和时序，不把现有 16 个 PE 的 8 拍排短。
- Auto-DOK：编译期挑选适合放到 HBM 旁边的核。1360 个权重已在片上供给器里，584 拍在取指之后。
- MaxEmbed：用副本提高 SSD 大表的有效带宽。查找中位 33，盖不住 584。
- Jiang 的向量系统学位论文：查表变快之后把 DNN 放到 GPU 集群。多卡拆全连接已停。
- Boutros 学位论文：NPU 仍是矩阵向量单元后面接独立的逐元素单元，再映射到硬张量块。这是已经停掉的普通拆分。

没有新支线。没有 RTL，没有上板，没有改已验收 RTL。
